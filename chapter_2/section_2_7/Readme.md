### Exercise 2-7.

The operation to insert before is slightly more complicated, because we need to find two items: the one that matches, and the one immediately before. This means that, unlike with insertafter, we cannot simply reuse the function lookup, because while searching for the matching element we need to keep track of the previous element.



### Exercise 2-8.

Solution in the source files from this section. 



### Exercise 2-10.

Tests are included in the file section_2_7.c.
